# Persistence adapters (AD-1, AD-3). This is the ONLY layer permitted to import
# boto3 / touch DynamoDB. One repository per aggregate (Products, Carts, Orders),
# added by later stories. Nothing here yet — the layout is the contract.
